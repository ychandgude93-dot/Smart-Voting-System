# Copy to .env and fill with real values
DB_HOST=localhost
DB_USER=root
DB_PASS=your_db_password
DB_PORT=3306
DB_NAME=smart_voting_system
SENDER_ADDRESS=your_email@example.com
SENDER_PASS=your_email_password
SECRET_KEY=replace-this-secret
ADMIN_EMAIL=admin@voting.com
# Recommended: store hashed password (generate using werkzeug.security.generate_password_hash)
# Generate a password hash with: python code/generate_admin_hash.py yourpassword
ADMIN_PASSWORD_HASH=
# Set to 1 in production when running under HTTPS
SESSION_COOKIE_SECURE=0
FLASK_DEBUG=0
